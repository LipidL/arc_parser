{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Compile and  Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/${workspaceFolderBasename}",
            "args": ["-f","100.arc","--surface","19","--surface","226","--surface","91"],
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "rust: cargo build"
        }
    ]
}
