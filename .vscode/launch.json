{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Compile and  Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/${workspaceFolderBasename}",
            "args": ["-f","small_Wulff_init.arc","--surface","85","--surface","86","--surface","167"],
            "cwd": "${workspaceRoot}/test",
            "preLaunchTask": "rust: cargo build"
        }
    ]
}
